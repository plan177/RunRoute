# RunRoute — Правила разработки

## Перед изменениями

- Прочитать `README.md` и затронутые файлы
- Понять архитектуру и текущее поведение

## Ветки и PR

- Одна задача — одна ветка и один PR
- Начинать ветку от актуального `master`
- Название ветки: `feat/`, `fix/`, `refactor/`, `docs/`, `perf/`

## Общие правила

- Менять только компоненты, относящиеся к текущей задаче
- Не делать массовое форматирование
- Не добавлять зависимости без явной необходимости
- Не коммитить `.env`, токены и ключи
- Не смешивать рефакторинг и новую функциональность

## При bugfix

1. Описать причину бага
2. Описать как баг воспроизводится
3. Исправить
4. Перечислить проверки

## При рефакторинге

1. Подтвердить, что поведение не изменено
2. Перечислить затронутые функции
3. Проверить сценарии из README.md

## После изменений

- Перечислить проверки в описании PR
- Убедиться, что auto/manual/track работают
- Убедиться, что экспорт и шаринг работают

## Файлы проекта

| Файл | Назначение | Доступ на изменение |
|------|-----------|---------------------|
| `bot.py` | Telegram-бот | Только если требуется задачей |
| `mini-app/app.js` | Клиентская логика | Только если требуется задачей |
| `mini-app/index.html` | Разметка | Только если требуется задачей |
| `mini-app/style.css` | Стили | Только если требуется задачей |
| `backend/` | API сервер | Только если требуется задачей |
| `requirements.txt` | Зависимости | Только если требуется задачей |
| `.env.example` | Пример конфигурации | Да |
| `.gitignore` | Правила игнорирования | Да |
| `README.md` | Документация | Да |
| `CLAUDE.md` | Правила разработки | Да |
